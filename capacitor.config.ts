import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'To-DoListSimple-Angular',
  webDir: 'www'
};

export default config;
